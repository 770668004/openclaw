#!/bin/bash
# 核心记忆保存命令

if [ $# -eq 0 ]; then
    echo "用法: memory-core \"核心指令内容\""
    exit 1
fi

python3 /home/kousoyu/.openclaw/workspace/skills/multi-memory-manager/memory_manager.py core --content "$*"